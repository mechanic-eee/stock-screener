# 갭 감사 — 며칠 운영 후 검증 (2026-06-27)

> 멀티에이전트 6 에이전트(프로덕션·코드·서비스·데이터 갭 발견 → 종합 → 적대적 검증)가 **실제 DART/yfinance fetch·실공시 디코딩**으로 검증. 적대적 검증이 severity·범위를 일부 교정.

## ✅ 프로덕션 검증 (정상)
- **유동성하한 라이브 작동** — 후보 3412 → 1808(KR574·US1234), 잡주 47% 제거. (단, turnover가 KR에서 adj_close×raw_volume 혼용 — 분할종목 미세 왜곡, US는 auto_adjust=False라 정합)
- **health.json 발행**, daily-scan 06-24~26 success, data브랜치 06-26 최신.
- valuation 99%·fundamentals(번들존재) 97.5%.
- benchmark/RS·KR valuation은 구스키마 영향 없음(확인).
- 코호트: 5/25 base-only(33d) −12.1%/30% vs 6/25 enrichment(2d) +3.2%/70%(잠정).

## 🔧 이번에 수정 (커밋 1bbb8dd, 1048d49)
| # | 갭 | 검증 | 수정 |
|---|---|---|---|
| 0 | 신규 펀더신호 프로덕션 7%만 채움 | 80일 캐시에 5월 구스키마 행(shares 52% vs 신규 7%가 증거) | `_SCHEMA_CUTOFF`로 1회 강제 재fetch |
| 1 | **데이터없음 펀더필터 full-weight 희석** | base-90이 펀더6개 unavailable이면 53.9(→90이어야) | `if out.available` 가드 — 검증: base-only 100==unavailable 100 |
| 2 | **GP/accruals 분기분자 vs 연간임계** | AAPL GP점수 49(세계최고가), 83%가 1~25밴드 | 연율화(US TTM·KR YTD-scale) — AAPL GP 49→**100** |
| 3 | **bsnSp 오탐** | 호텔신라(면세1구역 10.9%)를 부도로 제외 | sl_vs≥50%만 치명 — 호텔신라 제거·대우건설(72.8%) 유지 |
| 4 | **daily 알림 base-only 랭킹** | 100점 falling-knife 동률을 매일 푸시(프로젝트가 부정한 모집단) | enrichment 합성점수 랭킹(사이드카 warm, fetch 0) |
| 5 | **health가 번들존재만 측정→7% 사고 못잡음** | dead-man-switch가 정작 이 사고를 놓침 | signal_fill% + 텔레그램 건강라인(⚠️ if <80/50%) |

## ⏳ 검증 대기 (다음 스캔 후 확인)
- 오늘 밤 스캔이 `_SCHEMA_CUTOFF` 재fetch + 연율화로 신규신호를 채우는지: `gh run list` 후 사이드카에서 f_score/altman notna가 US ~90%로 오르는지. (이제 텔레그램 건강라인 신규신호%로도 즉시 보임.) **순서 주의:** 캐시 채워진 뒤에야 enrichment 알림이 4개 신규신호까지 온전히 반영(그 전엔 fundamental+valuation+RS 0.55 가중으로 랭킹 — base보단 월등).

## 📋 남은 백로그 (우선순위)
**[High] KR distress 구조적 약화** — `_fetch_kr`이 2분기(당기+전년동기)만 fetch → `four_quarters_all_loss`가 KR에서 결코 발화 못함(565 KR 전부 0). + `audit_qualified`는 관리종목이 유니버스에서 선제외돼 생존자엔 구조적으로 거의 안 잡힘. → KR 치명게이트가 capital_impairment + (정밀화된)risk_event에만 의존. **fix:** KR 직전 4분기 보고서 fetch해 4Q적자·YoY추세 평가가능하게.

**[Medium] 재발방지·정리:**
- `_SCHEMA_CUTOFF` 날짜상수 → row별 `schema_version` int로(다음 컬럼추가 시 자동무효화, 수동 bump 잊으면 또 사일런트 7% 재발).
- `audit_qualified` per-ticker DART콜이 구조적으로 거의 안 발화 → belt-and-suspenders로 문서화하거나 콜 제거(DART 절약).
- turnover를 미조정 close×volume로 통일(KR 분할종목 정합).
- **track.py 자동화 없음 + 클라우드 live-fetch 차단** — 코호트 추적이 수동·TRACKING.md stale. data브랜치 candidates.parquet 종가를 1차소스로(fetch 0) + daily-scan 끝에 track 스텝.

**[Medium] 일일 루프 마찰(DECISIONS 0건):**
- `scripts/decide.py`(--ticker NVO → 워치리스트 행 파싱→position_size→DECISIONS append, 전사 0회) + `daily.bat`(track+큐레이션 환기 한 번에). 결정기록 유도장치 부재가 점수실효성 검증을 막음.

**[Low]** WATCHLIST 큐레이션 환기(stale 관심행 리스트업) / 온보딩 단일진입점.

## 핵심 메시지
운영 며칠 만에 **신호 품질·점수 정합성 버그 다수**가 드러났다(희석·연율화·오탐). 전부 "신호가 존재하나 옳지 않은" 부류 — 사이드카 채움률·점수분포를 봐야 잡힌다. 이번 health signal_fill·텔레그램 건강라인이 앞으로 이런 사일런트 저하를 푸시로 잡는다. 다음 레버는 **KR distress 4분기 fetch + 일일 루프 마찰 제거(decide.py)**.
