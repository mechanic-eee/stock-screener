# 시스템 평가 — 2026-09-05 (Fable 5.1 프레시-아이 리뷰)

> 대상: stock-screener(코드·파이프라인·자동화) + stock-investing(프로세스·저널). 방법: 실측(테스트·CI·예약작업·로그·DB) + 3 에이전트 병렬 검토(코드 견고성 / 투자 프로세스 설계 / 스코어링·데이터) + 🔴 전건 코드 재확인.
> 결론 한 줄: **규칙과 판정 로직은 신뢰할 만하다. 빠진 것은 세 층 — ① "결과가 폰에 도착했다"는 보증, ② 재무 데이터의 신선도, ③ 12주가 약속한 측정(준수율·대조군·계좌 히트).**

## 1. 실측 상태

| 항목 | 결과 |
|---|---|
| 회귀 테스트 `tests/test_recommend.py` | 15/15 통과 · `selftest.py` 통과 |
| CI Daily scan (GitHub Actions) | 최근 10런 success, 약 65분, 시작 지터 22:19Z~05:54Z |
| 예약작업 2건 | LastResult 0, StartWhenAvailable=True, **WakeToRun=False** |
| **로컬 실행 커버리지 (8/10~9/4 평일 20일)** | 정시 13 · 당일 지연 2 · 익일 catch-up 2 · **무실행 3~5** (8/13·8/24·8/25 + 8/31·9/4는 익일 새벽) → **정시 65%, 당일 75%** |
| 워치독 경보 | 2건(8/8·9/1) — PC가 꺼진 날은 감시자도 꺼져 미검출 |
| 스냅샷 | 시세 2026-09-04, 1,613종목, 펀더 fill 97% |
| **KR 재무 기준 분기** | **전부 2026-03-31 (1분기)** — 9/5 재조회분 포함. 반기(6/30) 0행 |
| 페이퍼 손절 집행 지연 | MNSO 8/27 이탈 → 9/5 기록 (**6거래일**, 그동안 monitor가 매일 같은 경보) |
| 로그 | `monitor-log.txt` 파이썬 출력 전부 cp949 깨짐 → 전송 성공/실패 판독 불가 |

## 2. 유지할 것 (잘 설계된 부분)

- **음성 결과가 운영 금지사항으로 배선됨** — 타이밍 기각이 문서가 아니라 review 철학·recommend 금지표·"눌림 대기 금지"로 강제. 개인 프로세스에서 드묾.
- **fail-closed 기본값** — 가격 None=🔴/⚪, 레짐 None=신규 차단, 시세 2영업일 초과=중단, 스냅샷 발행 가드(생존<100·시장 0·OHLC 불일치 abort).
- **결측-중립 희석 가드**(`engine.py` `available=False` → 가중합 제외), PIT 백테스트 정직성(`filed<=T`, DART FY 익년 4/1).
- **코호트 무결성 집착**과 베토군 사후추적 의무 — 사람 레이어를 반증 가능하게 만듦. MNSO 반사실 즉시 기록.
- **계좌 전체를 규율 아래로** — 시스템 밖 보유(파두·SNDK·레버리지)까지 출구규칙 강제, 레버리지 "미루기 대차" 달러 실측.
- 시크릿 위생(토큰 미로깅·개인 JSON 5개 gitignore·원자 쓰기), 안정 셸 경로·독립 도메인 워치독.

## 3. 발견 사항 — 우선순위 통합

심각도: 🔴 잘못된 매매 판단 또는 무음 장애 유발 / 🟠 수개월 내 비용 / 🟡 위생. 공수: S(반나절 이하) M(1~2일).

### P0 — 이번 주 (실돈·판정 직결) — ✅ 전건 완료 2026-09-05 밤 (커밋 83a2a41·2ff53e0·431b189·83ecfe8)

1. 🔴 **KR 재무가 1분기에 고착** — `fundamentals.py:593` 후보 리스트에 당해년 `11012`(반기)·`11014`(3분기) 없음. 8/15 이후 KR 핵심 4신호·밸류·부도판정 전부 3월 데이터. 파두 랭킹(355/459)도 이 데이터. **+ 펀더 as-of 무표기**(사이드카에 period/fetched_at 없음 → 두 달간 아무 표면에도 안 보였음). 수정: 후보 순서 `(ty,11014),(ty,11012),(ty,11013),(ty-1,11011)…` + recommend 헤더/텔레그램 건강라인에 "펀더 기준: KR 2026-06-30 / US 2026-06-30". **S~M**
2. 🔴 **시세 신선도가 봉 날짜가 아니라 fetched_at 기준** — `track._current_price`가 마지막 봉 날짜를 안 봄. 거래정지 종목이 옛 종가로 매일 🟢. 게다가 review 0.5일 vs monitor/track 1.0일 불일치인데, 실계좌 시스템 픽(NVO·SIRI) 알림은 review가 제외하고 **monitor 단독**(가장 느슨한 캐시). 로그 실증: 같은 런에서 SIRI $28.46 vs $29.12. 수정: `(price, bar_date)` 반환, 영업일 2일 초과=None+"시세 정지 N일", `PRICE_MAX_AGE_DAYS` 상수 1개 공용. **S**
3. 🔴 **워치독 성공 판정 = "pwsh가 시작했다"** — `Start-Transcript`가 파이썬 실행 전에 로그를 쓰고, 텔레그램 전송 실패도 exit 0. venv 파손·토큰 폐기·30분 타임아웃(review가 맨 마지막 단계) 전부 "OK". **+ PC 꺼짐은 두 도메인 공통 실패**(4주간 무실행 5일, 경보 1건). 수정: review가 `_send()` 성공 직후에만 `data/last_heartbeat.json` 기록 → 워치독은 그 파일 검사; 전송 실패=exit 2; review를 첫 단계로; 외부 dead-man(healthchecks.io 핑 1줄, 09:00까지 없으면 그쪽이 알림). **S**
4. 🔴 **일일 텔레그램 랭킹·to_watchlist에 결측 게이트 없음** — `daily_scan.py:207` 알림 경로는 유형 필터뿐. 9/5 export 실증: KR 1위 317450(piotroski 결측), US MCHB(4신호 중 3개 결측, 87점)가 폰에 "상위"로 감. 부수: `fundamental` 필터가 `available=False`인데 `value=50.0`을 내보내 FUND4 결측 검사에 영원히 안 걸림(사문). 수정: FUND4 결측 헬퍼 공용화 → 알림 행 `⚠️결측n` 태그/제외, `fundamental` 미가용 시 `value=None`. **S**
5. 🟠 **포트폴리오 히트·상관 한도가 실계좌에 없음** — NAND(파두+SNDK) 65%가 7주째 "comfort 경고"만. 파두 오픈리스크(현재가−스톱)×수량 ≈ 계좌 10% = 시스템 R1% 옆에 10R. 설계 §1 단계6(오픈R≤6%·슬리브≤60%)은 어느 스크립트도 계산 안 함. 수정: review에 `히트 Σ(가격−스톱)×수량/총자산 · 보유수 · 테마` 한 줄 + 초과 시 신규 실계좌 진입 차단 플래그. **S**

### P1 — 9/12 페이퍼 8주 평가 전 (측정이 없으면 평가가 없다) — ✅ #5·#6·#7·#9 완료 2026-09-06 (#8 대조군 자동 시드·#10 랭킹 신선도는 9월 중)

6. 🟠 **준수율을 아무도 세지 않는다** — 12주의 공식 목표인데 위반 증거는 산문에만(2차 8/10→8/17 지연, NVO 2차가 가격조건부로 변질, 사이클 #2·#3 미실행). 수정: 계획이벤트 표 1개(종목·행동·예정일·실행일·이탈코드), 과거분 1회 수작업 복원(~40건). **S**
7. 🟠 **결정로그가 채굴 불가 산문 + 팁 10건은 추적 안 됨** — 판정어 제각각, 결정가 결측(KLC·CABO), 최신순 깨짐(8/28이 9/5 위). 팁은 WATCHLIST에 없어 track이 안 봄 → "10/10 기각이 옳았나" 측정 불가. 수정: 항목 끝 트레일러 `| verdict= | src=funnel/tip | px= | recheck= | code=` + `decide.py`가 `decisions.jsonl` 병기 + 팁·베토 전 종목 결정가로 `[대조군]` 코호트 소급 시드. **S**
8. 🟠 **대조군 부재** — 진짜 반사실은 KOSPI가 아니라 "같은 주 게이트 통과했지만 안 뽑은 종목"·"게이트 탈락 종목". exports 7개에 다 있는데 전향 추적 없음. 수정: recommend가 매 사이클 후보·탈락을 `[대조군]` 코호트로 자동 시드. **S~M**
9. 🟠 **페이퍼 손절이 Claude 세션에 의존** — 페이퍼는 전결인데 집행은 사람이 세션을 열어야 됨(MNSO 6거래일). 수정: monitor가 페이퍼 포지션 종가 이탈 감지 시 `decide.py --action 청산`을 익일 종가로 자동 호출(실계좌는 알림만). **S**
10. 🟠 **랭킹 체크에 스냅샷 신선도 게이트 없음** — review/monitor가 `_load_rows` 직접 호출로 recommend의 2영업일 게이트 우회. Actions 무음 중단 시 낡은 "하위 50% 강등"이 그대로 나감. 수정: asof 반환·표기, 초과 시 "⚪ 랭킹 미수행(낡음)". **S**

### P2 — 10/10 분기 리뷰까지 (점수 정의는 동결 대상 → 섀도 비교 후 전환)

11. 🟠 **라이브 점수가 검증된 정의와 어긋남 3건** — (a) Altman EBIT 미연율화(백테스트는 연간 EBIT) → Z'' 27~67점 오차, 부도 문턱 과다 발화 (b) 부채 정의 3곳 상이: US 라이브 `Total Debt`(이자부채) vs KR·백테스트 `부채총계` → US 체계적 유리 (c) KR "4분기 연속적자" lethal이 캐시 누적(Q1·FY·Q1·FY)으로 발화 — 246710 실증, WONTFIX 결정과 충돌. **이건 규칙 변경이 아니라 구현 드리프트 수정**이지만 랭크 기반 규칙(하위 50%·상위 25%)에 영향 → 코드 수정 후 보유종목 랭크 old/new 섀도 비교 → 10/10 전환. **S each**
12. 🟠 **KR 확정 위험신호가 오류 시 "깨끗"** — DART 감사의견·위험공시 fetch 예외 → False/None(=정상), 관리종목 목록 fetch 실패 → 그 주 유니버스에 관리종목 전원 포함. 수정: 3상태 필드(`audit_qualified: Optional`, `risk_checked_at`), 실패 시 직전 캐시 유지+경고. **M**
13. 🟠 **KR 장중 부분봉 유입 경로** — cron 지터로 10런 중 4런이 KST 장중 시작, FDR fetch에 세션 가드 없음. 수정: 06:40Z 전 당일 KR 행 drop 또는 cron 21:00Z. **S**
14. 🟠 **상태 파일 이력·백업 0 + 진실 원천 4중** — stock-investing은 git 아님, DECISIONS 비원자 전체 덮어쓰기, holdings/DECISIONS/보유현황/ledger 정합성 검사 없음(NVO 손절을 holdings만 올리면 monitor는 옛 손절로 감시). 수정: stock-investing 로컬 `git init` + daily 끝 auto-commit, decide `os.replace`, review에 system:true stop vs DECISIONS 비교 1줄, decide 청산→ledger append, 보유현황.md 생성물화, 재검토 조건(~10건 산문) 전부 watch로 이관. **M**
15. 🟡 **위생 묶음** — 로그 UTF-8(`[Console]::OutputEncoding`+`PYTHONIOENCODING`), 메시지에 실행시각+catch-up 표기, EDGAR seen 손상 시 "기준선 재설정" 표면화+monitor 원자 쓰기, `common.py`(current_price/fmt/market_of/FX 1460 5곳/send/ALERT_SET 3곳/`점수` 리터럴 24회), decide 고정 인덱스 파싱→track `_col` 재사용, 공개 저장소 이메일→`.env`, 알림 레짐 라인 실패 시 "판정불가 ⚠️", 유니버스 50/45 히스테리시스·동점 2차 키·소규모 주 가드, `--indicators all`이 기각 필터를 하드게이트로 켜는 경로 제거. **S each / M 합계**

## 4. 10/10 분기 리뷰가 답해야 할 5가지 (데이터 소재)

| 질문 | 소재 |
|---|---|
| 계획 행동 대비 실행률(즉시진입·2차 날짜·손절 당일·베토≤2/주·주간 사이클) | **없음** → #6 |
| 사람·에이전트 레이어가 가치를 더했나(채택 vs 베토/보류 vs 게이트통과 전체) | 베토군 3종만 → #7·#8 |
| 페이퍼→실계좌 마찰(제안가↔체결가·스냅샷 낡음·비용 후 순엣지) | 원자료 있음(DECISIONS·ledger), 집계 없음 → 포지션 표 `proposal_px` 열 |
| 계좌 수준 리스크(자산곡선·실현 R배수·히트/테마초과 일수) | ledger 6건. 자산곡선 없음(portfolio.json 1회 스냅샷) → #5 + 일일 자산 1행 |
| 재량 파라미터(ATR 8%·결측·랭크컷 15) 탈락 종목 사후 성과 | exports 7개에 있음 → 스크립트로 파생 |

핵심: n이 작아 규칙 개정은 거의 정당화 안 됨. **이 리뷰의 산출물은 규칙 변경이 아니라 측정 변경.**

## 5. 테스트 갭 (우선)
`review._run` 오케스트레이션(격리·system 제외·seen 손상) · 시세 신선도(봉 날짜·max_age) · 텔레그램 실패→exit 사슬 · `_fetch_kr` 보고서 선택 · `fundamental` 결측 FUND4 · Altman/부채 정의 방향 · daily_scan 신선도 abort. 부수: 테스트가 pytest 발견이 아니라 `main()` 수동 목록 — 새 함수 누락 시 조용히 미실행.

## 6. 추천 실행 순서
1. **P0 #1~#4** (합계 ~1.5일): 데이터 신선도 두 축 + 도착 보증 + 알림 결측 게이트. 전부 "옳은 입력 위에서 돌았고 폰에 도착했다"를 보증하는 층.
2. **P0 #5 + P1 #6·#7·#9** (~1.5일, 9/12 전): 계좌 히트 한 줄, 계획이벤트 표, 결정로그 트레일러+대조군 소급, 페이퍼 손절 자동 집행.
3. **P1 #8·#10, P2 #11 섀도** (9월 중): 대조군 자동 코호트, 랭킹 신선도, 점수 드리프트 수정 코드+비교표(전환은 10/10).
4. **P2 #12~#15** (10월): 3상태 위험신호, 장중 가드, git init·정합성, 위생 묶음.
